<Page
    x:Class="SyncBoard.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SyncBoard"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel x:Name="HeaderPanel" Orientation="Horizontal" Grid.Row="0">
            <TextBlock x:Name="Header"
        Text="Ypsilon SyncBoard"
        VerticalAlignment="Center"
        Style="{ThemeResource HeaderTextBlockStyle}"
        Margin="10,0,0,0" />
        </StackPanel>
        <Grid Grid.Row="1" Margin="0,69,0,0">
            <!-- Canvas for displaying selection UI. -->
            <Canvas x:Name="selectionCanvas" Margin="0,53,0,0">
            </Canvas>
            <!-- Inking area -->
            <InkCanvas x:Name="inkCanvas"/>
        </Grid>
        <StackPanel Margin="0,61,0,871" Grid.RowSpan="2">
            <ComboBox Width="120" SelectedIndex="0">
                <ComboBoxItem Content="Standard" Tapped="ComboBoxItem_Tapped"/>
                <ComboBoxItem Content="Rot" Tapped="ComboBoxItem_Tapped_1"/>
            </ComboBox>
        </StackPanel>
    </Grid>
</Page>
